<%# TODO: maybe first string is redundant, but I use it for understanding and clear %>
<% ticket = item %>

<%= table_cell ticket.name %>
<%= table_cell ticket.user_name %>
<%= table_cell ticket.train.number %>
<%= table_cell ticket.user.present? ? ticket.user.name : "Пользователь не найден" %>
<!--TODO: replace to controller -->
<%= table_cell Station.find_by(id: ticket.station_first).name %>
<%= table_cell Station.find_by(id: ticket.station_last).name %>

<%= render 'components/table_options', item: ticket, edit_item_path: edit_ticket_path(ticket) %>